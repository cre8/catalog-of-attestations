{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.example.org/attestations/gym-membership-card/1.0.0/dc+sd-jwt.json",
  "title": "Gym Membership Card (SD-JWT VC)",
  "description": "Logical claim set for the Gym Membership Card SD-JWT VC.",
  "type": "object",

  "properties": {
    "vct": {
      "type": "string",
      "description": "Verifiable Credential Type identifier for this attestation.",
      "const": "https://gym.example.com/attestations/gym-membership-card"
    },

    "gymName": {
      "type": "string",
      "description": "Human-readable name of the gym that issued the membership.",
      "minLength": 1,
      "maxLength": 256
    },

    "memberId": {
      "type": "string",
      "description": "Internal membership identifier, unique per issuer.",
      "minLength": 1,
      "maxLength": 128
    },

    "membershipTier": {
      "type": "string",
      "description": "Membership tier defining included services and pricing.",
      "enum": ["Basic", "Standard", "Premium", "VIP"]
    },

    "nbf": {
      "type": "string",
      "description": "Date from which the membership is valid (inclusive).",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    },

    "exp": {
      "type": "string",
      "description": "Date until which the membership is valid (inclusive), assuming it is not revoked.",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    }
  },

  "required": [
    "vct",
    "gymName",
    "memberId",
    "membershipTier",
    "nbf",
    "exp"
  ],

  "additionalProperties": false
}
